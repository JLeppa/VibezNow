{% extends "mybase.html" %} <!--this means that you are extending the base tempate -->
{% block user_id %} <!-- this is the name of the block below -->
       <div class="row" style="height:100vh;">
               <div class="col-md-4 col-md-offset-4" style="margin-top:10vh;">
                       <form action"user_op" method="POST">
<!-- action is the html template that will return the result to this input query -->
                               <div class="form-group text-center">
                                       <label style="font-weight:300; font-size:36px;">Hello!</label>
                                       <br /><br />
                                       <input type="text" class="form-control" id="user_id" name="user_id" placeholder="type your name here">
                                       <br /><br />

                                       <button type="submit" formmethod="post" value="Send" name="user_id-container" class="btn btn-default id-submit">Submit</button>
                               </div>
                       </form>
               </div>
       </div>

{% endblock %}
